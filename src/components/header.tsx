import React from 'react';
import styled from 'styled-components';

const HeaderText = styled.h1`

`

function Header() {
    return(
        <HeaderText>Marketing Image Resizer</HeaderText>
    )
}

export default Header;